# Base URL of the FastAPI backend (physical_ads_as_a_service).
# Locally, leave unset and the frontend will hit http://localhost:8000.
# Set in Vercel for production:
NEXT_PUBLIC_API_BASE_URL=https://penpal-opportunity-report.fly.dev
